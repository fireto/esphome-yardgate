# Enable wifi
wifi:
  ssid: !secret vnAutomationWifiSsid
  password: !secret vnAutomationWifiPwd
  # ssid: !secret vnCommonWifiSsid
  # password: !secret vnCommonWifiPwd
  
  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "$upperDevicename Hotspot"
    password: !secret fallbackWifiPwd
    
captive_portal:
